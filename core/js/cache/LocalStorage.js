/**
 * @module 本地存储基类[本地存储基类]
 * @description 本地存储基类，不可直接创建对象
 *
 * @author:
 * @date: 2013-09-10 下午10:32
 */
define(["core/js/Class"], function (Class) {
    var LocalStorage = Class.extend({
        set: function (key, value, seconds) {
        },
        get: function (key) {
        },
        remove: function (key) {
        },
        clear: function () {
        },
        getAll: function () {
        }
    });

    return LocalStorage;
});